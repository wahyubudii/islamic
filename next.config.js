/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // env: {
  //   JWT_PRIVATE_KEY: 7process.env.JWT_PRIVATE_KEY
  // }
}

module.exports = nextConfig
